[llm]
provider = "{{LLM_PROVIDER}}"
api_key = "${LLM_API_KEY}"
model = "{{LLM_MODEL}}"
base_url = "https://api.deepseek.com/v1"
temperature = 0.7
max_tokens = 4096

[langfuse]
public_key = "${LANGFUSE_PUBLIC_KEY}"
secret_key = "${LANGFUSE_SECRET_KEY}"
host = "https://cloud.langfuse.com"

[interfaces]
enabled = {{ENABLED_INTERFACES}}

[interfaces.web]
host = "0.0.0.0"
port = 8080

[interfaces.telegram]
bot_token = "${TELEGRAM_BOT_TOKEN}"

[agent]
checkpoint_db = "data/checkpoints.db"
max_history = 50

[skills]
enabled = []

# Optional: override individual skill configs
# [skills.config.web-search]
# tavily_api_key = "${TAVILY_API_KEY}"
# max_results = 5
#
# [skills.config.financial-data]
# default_market = "sh"
